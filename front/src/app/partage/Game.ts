export interface Game {
  _id?: string,
  miniature?: string,
  nom?: string,
  status?: string,
  note?: number,
  plateforme?: string[],
  support?: string[],
  commentaire?: string,
  favoris?: boolean
}
